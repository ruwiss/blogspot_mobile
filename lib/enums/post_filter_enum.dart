/// Menü için
enum PostFilter { posts, pages, drafts }

/// Filter butonu için
enum OtherFilter { ascending, descending, scheduled, defaultValues }

/// artan, azalan tarih sıralaması
enum SortOption { ascending, descending }

/// Paylaşım durumu
enum PostStatus { draft, live, scheduled }
